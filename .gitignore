# dependencies
/node_modules

# testing
/coverage

# next.js build files (remove if you want to track them)
/.next/
/out/

# production build (remove if you want to track it)
/build

# misc
.DS_Store
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (keep private, but remove from .gitignore if you want to track them)
.env*.local
.env

# vercel (remove this if deploying via GitHub and want to track configuration)
# /.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts
